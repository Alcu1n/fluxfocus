# Folder: fluxfocusTests

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时,更新此文档。

This folder owns focused unit tests for pure app-domain lifecycle rules, NFC tag snapshot encoding, NFC routing decisions, and user-visible violation contracts. / 本文件夹负责纯应用领域生命周期规则、NFC 标签快照编码、NFC 路由决策与用户可见违规契约的聚焦单元测试。
It consumes the full-app module through `@testable import fluxfocus` and only asserts stable value-level contracts, not SwiftData or UI rendering details. / 它通过 `@testable import fluxfocus` 消费完整 App 模块，并且只断言稳定的值级契约，而不测试 SwiftData 或 UI 渲染细节。
The invariant is that tests stay fast, deterministic, and isolated from simulator-host persistence teardown while guarding NFC-governed start/end state transitions and retired-rule visibility. / 最重要的不变量是测试必须保持快速、确定性，并隔离模拟器宿主持久化回收问题，同时守住 NFC 治理的开始/结束状态迁移与废弃规则可见性。

## Files
- `FluxFocusAppStoreTests.swift`: unit tests - session transitions, precedent reuse, snapshot encoding, NFC touch dispositions, and violation visibility / 会话迁移、判例复用、快照编码、NFC 触碰分支与违规可见性单元测试
