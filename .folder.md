# Folder: /

> Trigger: When this folder's structure/responsibilities/file list changes, update this document.
> 触发条件:当本文件夹的结构/职责/文件列表变化时,更新此文档。

This root folder defines the product map, Xcode wiring, and deployment boundary. / 根目录定义产品地图、Xcode 连接与部署边界。
App code lives in `fluxfocus/` and `fluxfocusClip/`; public web assets live in `docs/`. / App 代码位于 `fluxfocus/` 与 `fluxfocusClip/`，公网静态资源位于 `docs/`。
The invariant is a single short invocation protocol plus one matching full-app handoff scheme. / 最重要的不变量是统一的短 invocation 协议，以及与之匹配的一套完整 App handoff scheme。

## Files
- `README.md`: root map - macro architecture and invocation boundary / 宏观架构与 invocation 边界
- `PRD.md`: product source - requirements and MVP scope / 需求源文档与 MVP 范围
- `APPCLIP_DEPLOY.md`: deployment guide - Pages, Cloudflare, and Connect setup / Pages、Cloudflare 与 Connect 部署指南
- `fluxfocus/`: app folder - full iOS app runtime / 完整 iOS App 运行时代码
- `fluxfocusClip/`: clip folder - App Clip runtime / App Clip 运行时代码
- `docs/`: web folder - hosted AASA and landing assets / 托管 AASA 与落地页资源
- `fluxfocus.xcodeproj/`: project folder - targets, schemes, and signing settings / target、scheme 与签名设置
